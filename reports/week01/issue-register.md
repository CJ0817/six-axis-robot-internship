# 第1周问题表

更新日期：2026-09-15。登记范围为本轮安装与路径修复，验证环境为云端 Linux x86_64；关闭依据为提交 `7e5d6b1` 的[验证记录](install-path-validation.md)。

**本轮无未解决安装／路径问题。** 此结论限于本轮已验证范围，不代表后续算法已经完成。

## 安装／路径问题

| 编号 | 问题 | 状态 | 影响 | 处理方式 | 负责人 | 关闭条件 |
| --- | --- | --- | --- | --- | --- | --- |
| INSTALL-01 | 仓库外调用安装脚本时，相对 ROBOT_PYTHON 在切换目录后解析，找不到解释器 | 已关闭 | 安装启动失败，退出码127 | 共用预检查在切换目录前将解释器路径转为绝对路径，保留虚拟环境符号链接，支持空格路径 | 云端执行者 | 基础/基准安装脚本的仓库外相对路径预检查通过；空格路径回归通过；基础完整安装及构建通过。已满足，见 after-relative-python.log、regression.log、setup.log |
| INSTALL-02 | 未激活虚拟环境时，环境检查从 PATH 查找 CMake/Ninja，可能缺失或误用系统版本 | 已关闭 | 版本检查失败或误判环境，阻断验证 | 明确使用当前 Python 解释器同目录工具，并继续校验锁定版本 | 云端执行者 | 在 PATH=/usr/bin:/bin、未 activate 的条件下，使用虚拟环境 Python 完成版本检查。已满足，见 before-tool-path.log 与 after-tool-path.log |

上述日志位于 [results/path-validation](../../results/path-validation/)。如同类问题在新环境复现，重新打开对应编号并补充环境、复现命令和日志。

## 2026-09-17 收尾补充

| 编号 | 问题 | 状态 | 影响 | 处理方式 | 负责人 | 关闭条件 |
| --- | --- | --- | --- | --- | --- | --- |
| RUNTIME-01 | 云端运行时缓存的Python3.11.9文件缺失，旧虚拟环境符号链接失效 | 已关闭 | 本轮测试无法启动 | 恢复同版本解释器并重跑现有7项检查，不修改依赖锁 | 云端执行者 | 锁定解释器恢复、7项available检查通过；已满足，见day02-closeout.md |

本轮无未解决安装／路径问题。新增逆解输入已冻结，求解算法未运行；ALG-01保持未关闭。C IK ABI与错误/不可达输入集在实现前补齐，不能用准备检查替代算法验收。

## 使用限制（非安装故障）

| 编号 | 问题／限制 | 状态 | 影响 | 处理方式 | 负责人 | 关闭条件 |
| --- | --- | --- | --- | --- | --- | --- |
| LIMIT-01 | 仓库移动到新路径后，旧虚拟环境与 CMake 构建缓存可能保留原绝对路径 | 使用限制，持续适用 | 直接复用旧环境/缓存可能导致解释器、工具或构建路径失效 | 在新路径重新创建 .venv、需要时创建 .venv-baseline，并重新生成 build；按安装说明运行，不复制旧缓存作为可用环境 | 迁移操作执行者（云端执行者／本地执行者，按实际执行环境） | 不按故障关闭；每次迁移后以重建、版本检查、构建和相应测试通过作为该次操作完成条件。只有专门实现并验证迁移能力后才可撤销此限制 |

操作说明见[安装与路径排查](../../docs/install-path-troubleshooting.md)。

## 后续算法待办（不计入安装故障）

以下负责人为计划执行角色，具体字段及确认节点以[工程规范第7.5节](../../docs/engineering-contract.md)和[指标表](../../docs/test-metrics.md)为准，不表示已启动实现。

| 编号 | 问题／待办 | 状态 | 影响 | 处理方式 | 负责人 | 关闭条件 |
| --- | --- | --- | --- | --- | --- | --- |
| ALG-01 | URDF/DH映射及C正运动学已完成；雅可比与IK待完成 | 待实现／确认 | RTB 内置 UR5 旧数据因 d1 差异不可直接用于验收；显式项目参数对照已可用，C 算法待实现 | 完成模型对齐，补齐 FK、雅可比及解析 IK 接口与实现 | 云端执行者 | BASE-02 及适用 KIN 指标验收通过；普通、宽初值、近奇异组按统计规则分别报告 |
| ALG-02 | 三次/五次/梯形、直线/圆弧和避障待完成 | 待实现 | 尚无完整正式规划能力 | 按接口扩展计划实现并按方法验证连续性与约束 | 云端执行者 | 适用 PLAN 指标通过，待定指标先冻结；保存路径、约束与碰撞检查证据 |
| ALG-03 | 增量 PID 及速度/加速度前馈待完成 | 待实现 | P 辅助基线不能替代正式控制验收 | 冻结输出模式、离散式、增益单位和状态迁移，补齐控制实现 | 云端执行者 | 正式 CTRL-02/03/04 验收通过；保存 PyBullet 跟踪、故障与周期证据 |

关闭任何算法待办均需实际结果，不得仅凭文档、依赖安装或占位接口标为完成。

2026-09-17 模型核对更新：见[核对报告](ur5-model-audit.md)。RTB 内置 UR5 的 d1 差异已通过显式项目参数参考模型处理；不修改第三方包。仿真加速度限值仍待云端执行者在第3周规划接口冻结前确认，关闭条件是记录来源/适用范围并完成约束验证；此项为算法配置待办，不是安装故障。

2026-09-17 后续更新：C FK功能项KIN-01/KIN-02已通过，见[c-forward-validation.md](c-forward-validation.md)。ALG-01保持未关闭，剩余雅可比、IK及性能验证；不再把C FK列为未实现。

## 2026-09-19 导师反馈补充

| 编号 | 问题 | 状态 | 影响 | 处理方式 | 负责人 | 关闭条件 |
| --- | --- | --- | --- | --- | --- | --- |
| ABI-01 | C正逆解ABI未正式冻结 | 已关闭 | 上层易随接口变化调整 | 冻结ABI1.0.0、C头文件、布局静态断言及ctypes回归 | 云端执行者 | 现有FK兼容、布局/符号/错误码测试通过；已满足 |
| ROBUST-01 | FK边界和异常缺专项记录 | 已关闭 | 缺少鲁棒性证据 | 15边界+10进程隔离异常，均通过 | 云端执行者 | 返回码正确、边界正确、异常无崩溃/超时、输出不变；已满足 |
| PERF-01 | Python→C单次墙钟2.046632 ms超过1 ms门槛（1/11500）；C单次最大0.025369 ms通过 | 未关闭 | 整项性能尚未达标，不能宣称FFI单次延迟有保证 | 保留原始CSV；后续在固定负载主机结合调度/线程CPU时间定位，任何复测完整保留，不挑选结果 | 云端执行者 | 定位原因并在冻结条件下复验单次/p95门槛；若需求变更须先记录理由并获确认，不因测试失败直接放宽 |

详见[ABI与专项验证](../../docs/abi-v1-validation.md)。上述性能问题不属于安装故障，逆解仍未实现。
